fint:
  org-id: fintlabs.no
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: null
      client:
        registration:
          fint:
            client-id: "pwf.no"